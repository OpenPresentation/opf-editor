# Editor 0.6.0 release candidate

Branch `codex/shared-code-release-20260909` prepares unpublished editor 0.6.0 from tested integration `52e540b83e441892fd928e9f159bffbb29da5653`. All predecessors are published and registry-verified on Node 20.20.2/24.20.0: core 0.9.0 (`685733976a665d0190bdb9922b9a174ef94153d9`), renderer 0.7.0 (`f4a1b8e1324cbc182b98d43f16528594c8006f86`) and PPTX 0.7.0 (`f421c91b7b127c7eeab788acdc0c51969402e057`). The refreshed lock resolves these actual npm packages without unrelated upgrades.

The code browser fixture checks blank multiline code in wide and portrait canvases: the selection rectangle covers the accepted body height, real pointer editing works, a no-op preserves CRLF and avoids history, and undo restores the original source. Published renderer 0.7.0 includes the PR #11 trace-height correction. Both source and freshly installed candidate browser flows pass with Edge 152.0.4191.66. Four actual offline Roboto/Roboto Mono font faces are fingerprinted.

Clean installs, syntax/package checks, all nine model/component suites, the offline playground, code browser and fresh installed candidate checks pass on both runtimes. [Portable evidence](evidence/shared-code-release/summary.json) binds the identical local candidate integrity, all 32 byte-matched shipped files, exact registry predecessor integrities, fixture/bundle/font hashes and logs. Both installed workflows retain CRLF/source metadata, Tab/cancel, pagination/readability, undo/redo and exact code export/reimport; malformed inputs preserve the current document. npm reports zero advisories, 66 verified dependency signatures and seventeen attestations. The editor candidate itself is not registry-attested.

Packed checks and the trusted publication workflow include the code/blank-target browser cases. Complete final Linux Node 20/24 CI and review before exact-head merge and a fresh `opf-editor-v0.6.0` tag. Verify the actual npm package and immutable files on both runtimes after publication, then advance the complete-set plan, CI references and public deployments. Native formatting, theme, geometry and raster equivalence remain separate fidelity boundaries.
