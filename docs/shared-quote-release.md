# Editor 0.5.0 release candidate

Historical preparation record: editor 0.5.0 subsequently published from `dba5fe5e5580a4172c052132c4db5851d1decc4c` and passed actual registry verification. Do not republish it. The current successor is documented in [the shared-code release](shared-code-release.md); the candidate wording below records the earlier checkpoint.

Branch `codex/shared-quote-release-20260909` prepares editor 0.5.0 with core `^0.8.0`, optional renderer peer `^0.6.0`, and renderer/PPTX 0.6.0 for the example workflow. Editor 0.5.0 is unpublished. The original tested integration remains at `a143ee7b1c06b6cb7fbca482927661ca2b208226` on `codex/shared-quote-integration-20260909`. All predecessors are now published and verified: core 0.8.0 (`4dc292fa93bee52320bafa3fd0f5b05a2dc0a283`), renderer 0.6.0 (`7fe9905ad2d8a224efeef51b4a22d0aff0c413fe`) and PPTX 0.6.0 (`898e3c27919a2488e1e3d384168d6b25aae4bd5c`).

The refreshed lock resolves those actual npm packages without unrelated upgrades. A clean install, nine model/component suites, the offline playground workflow, metadata/syntax checks and fresh installed candidate checks pass on Node 20.20.2 and 24.20.0. One-page readability changes create history; repeated identical pagination is a no-op. Quote source edits, malformed-input preservation, export/reimport/undo and zero browser network writes pass on both runtimes with Edge 152.0.4191.66.

The new `npm run test:packed` builds a tarball and installs it with the exact registry predecessors and browser test tools in a fresh consumer. It byte-matches all 32 shipped files, links both legacy fixture runtime paths only to the installed dist, executes all nine model suites and bundles/runs the offline example using those installed bytes. It verifies no runtime files changed during the workflow. Both Node versions have identical candidate integrity, zero audited advisories, 66 registry dependency/test-tool signatures and seventeen attestations. The editor candidate is not yet registry-attested. [Node 20 evidence](evidence/editor-050-packed-node20.json) and [Node 24 evidence](evidence/editor-050-packed-node24.json) retain file/fixture hashes and exact predecessor integrities.

Linux CI and the publication workflow use the exact pinned Playwright 1.63.0 image verified in coordinated CI `34399051732`. Both now repeat the source example and installed-candidate browser gates as well as the model suite. Full CI/review remain gates before a fresh `opf-editor-v0.5.0` tag on the reviewed merge. Then verify the actual npm package/integrity/provenance, advance the complete release plan and immutable refs, execute all installed canvas suites, and adopt/reverify public deployments.

Native import preserves quote text as editable blocks but does not reconstruct the OPF quote structure, typography or readability policy. Full repair/Auto arrange, native pixel equivalence and font/multilingual completeness remain separate work.

Release review replaced the order-sensitive pagination string comparison with the module's existing structural JSON comparison. Public-operation checks cover nested policies in both JSON key orders and prove that no-op pagination preserves the original document and an existing redo entry. Current core behavior preserved key order in the inspected cases; the structural guard removes dependence on that implementation detail.
